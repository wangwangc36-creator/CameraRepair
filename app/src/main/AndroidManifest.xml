<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:label="CameraRepair 3.0.1"
        android:supportsRtl="true">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Safe hotfix: trims Camera2 face detection and video stabilization without blocking Meta native libraries." />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
    </application>
</manifest>
